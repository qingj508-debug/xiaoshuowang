apiVersion: apps/v1
kind: Deployment
metadata:
  name: endpoint-frontend
  namespace: endpoint
spec:
  replicas: 1
  selector:
    matchLabels:
      app: endpoint-frontend
  template:
    metadata:
      labels:
        app: endpoint-frontend
    spec:
      containers:
        - name: frontend
          image: 192.168.10.102/xiaoshuowang/frontend:c1a4f9516c9359d649a86e11d94d03bdb5b27db6
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: endpoint-frontend-svc
  namespace: endpoint
spec:
  type: NodePort
  selector:
    app: endpoint-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080
