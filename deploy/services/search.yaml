apiVersion: apps/v1
kind: Deployment
metadata:
  name: endpoint-search
  namespace: endpoint
spec:
  replicas: 1
  selector:
    matchLabels:
      app: endpoint-search
  template:
    metadata:
      labels:
        app: endpoint-search
    spec:
      containers:
        - name: search
          image: harbor.你的域名.com/endpoint-novel/search:latest
          ports:
            - containerPort: 10005
          envFrom:
            - configMapRef:
                name: endpoint-config
            - secretRef:
                name: endpoint-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: endpoint-search-svc
  namespace: endpoint
spec:
  selector:
    app: endpoint-search
  ports:
    - port: 10005
      targetPort: 10005
