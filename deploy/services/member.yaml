apiVersion: apps/v1
kind: Deployment
metadata:
  name: endpoint-member
  namespace: endpoint
spec:
  replicas: 1
  selector:
    matchLabels:
      app: endpoint-member
  template:
    metadata:
      labels:
        app: endpoint-member
    spec:
      initContainers:
        - name: wait-for-nacos
          image: 192.168.10.102/xiaoshuowang/busybox:1.36
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              until wget -q -O /dev/null http://nacos-service:8848/nacos/v1/console/health/readiness; do
                echo "waiting for nacos..."; sleep 3;
              done
      containers:
        - name: member
          image: 192.168.10.102/xiaoshuowang/member:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 10004
          envFrom:
            - secretRef:
                name: endpoint-secrets
            - configMapRef:
                name: endpoint-config
---
apiVersion: v1
kind: Service
metadata:
  name: endpoint-member-svc
  namespace: endpoint
spec:
  selector:
    app: endpoint-member
  ports:
    - port: 10004
      targetPort: 10004
